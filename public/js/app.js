// Generated by CoffeeScript 1.6.2
(function() {
  var Room;

  $(function() {
    var room_addr;

    if (!window.WebSocket) {
      alert("you brower is not support websocket");
      return;
    }
    room_addr = "ws://" + window.location.host + window.location.pathname + "/chatting";
    window.room = new Room(room_addr);
    room.room_addr();
    return console.log(room.connect());
  });

  Room = (function() {
    function Room(ws_url) {
      this.ws_url = ws_url;
    }

    Room.prototype.room_addr = function() {
      return console.log(this.ws_url);
    };

    Room.prototype.connect = function() {
      var w;

      return w = new WebSocket(this.ws_url);
    };

    return Room;

  })();

}).call(this);
